### 什么是 SQL 注入（SQL Injection）？如何防止？
* **原理**：攻击者通过在 Web 表单输入或 URL 参数中构造恶意 SQL 语句，欺骗数据库服务器执行非预期的命令（如删除表、窃取数据）。
* **防御**：
    * **预处理语句（Prepared Statements）**：最有效的手段。使用 PDO 的 `prepare` 和 `bindParam`，将 SQL 结构与数据分开，数据库会将参数视为纯数据而非可执行代码。
    * **严格的数据类型验证**：确保 ID 是整数等。
    * **转义特殊字符**：使用 `mysqli_real_escape_string`（不如预处理推荐）。
    * **最小权限原则**：数据库用户仅拥有运行业务所需的最小权限。

### 什么是 XSS（跨站脚本攻击）？有哪几种类型？如何防御？
* **原理**：攻击者在网页中注入恶意脚本（通常是 JavaScript），当其他用户浏览该网页时，脚本在用户浏览器中执行，窃取 Cookie、Token 或进行钓鱼。
* **类型**：
    * **存储型（Stored）**：恶意脚本存储在服务器数据库中（如评论区），危害最大。
    * **反射型（Reflected）**：恶意脚本包含在 URL 请求中，服务器返回时带回脚本并在浏览器执行。
    * **DOM 型**：纯前端漏洞，通过 JS 修改 DOM 结构触发。
* **防御**：
    * **输入过滤**：虽然需要，但不如输出转义可靠。
    * **输出转义（Output Encoding）**：在输出数据到 HTML、JS、CSS 上下文时进行编码（如 `htmlspecialchars` 将 `<` 转为 `&lt;`）。
    * **CSP（内容安全策略）**：通过 HTTP 头 `Content-Security-Policy` 限制浏览器只能加载指定域名的脚本，禁止内联脚本。
    * **HttpOnly Cookie**：禁止 JS 读取敏感 Cookie，防止 Session 劫持。

### 什么是 CSRF（跨站请求伪造）？它与 XSS 有什么区别？
* **原理**：攻击者诱导已登录用户访问恶意网站（或点击链接），利用用户浏览器中残留的 Cookie，向目标网站发送伪造的请求（如转账、改密）。
* **区别**：
    * XSS 利用的是用户对网站的信任（运行了恶意脚本）。
    * CSRF 利用的是网站对用户浏览器的信任（Cookie 自动携带）。
* **防御**：
    * **CSRF Token**：在表单或 Header 中加入随机生成的 Token，服务器验证 Token 是否匹配。攻击者无法获取第三方网站的 Token。
    * **SameSite Cookie 属性**：设置为 `Strict` 或 `Lax`，限制 Cookie 在跨站请求时不发送。
    * **验证 Referer/Origin**：检查请求来源域名。

### 什么是 SSRF（服务器端请求伪造）？
* **原理**：攻击者构造请求，由服务端发起请求。通常用于探测内网服务、攻击内网应用（如 Redis、MySQL）。
* **场景**：图片上传（远程 URL）、网页预览、Webhook。
* **防御**：
    * 过滤内网 IP 地址（127.0.0.1, 192.168.x.x 等）。
    * 限制请求端口（只允许 80/443）。
    * 禁止 30x 跳转，或在跳转时再次检查目标地址。
    * 使用 DNS 缓存防止 DNS 重绑定攻击。

### HTTPS 的工作原理是什么？
* **核心**：HTTP + SSL/TLS 协议。
* **过程**：
    1. **客户端 Hello**：支持的协议版本、加密套件、随机数 A。
    2. **服务端 Hello**：确认加密套件、随机数 B、下发数字证书（公钥）。
    3. **验证证书**：客户端验证证书有效性（CA 签名、有效期、域名）。
    4. **预主密钥**：客户端生成随机数 C（Pre-master secret），用服务端公钥加密发送。
    5. **生成会话密钥**：双方根据 A、B、C 生成对称加密的会话密钥。
    6. **加密通信**：后续数据传输使用会话密钥进行对称加密。
* **作用**：防窃听（加密）、防篡改（完整性校验）、防冒充（身份认证）。

### 密码应该如何安全存储？为什么不能用 MD5？
* **MD5 问题**：不再安全，计算速度过快，容易被彩虹表（Rainbow Table）破解或暴力破解；且有碰撞风险。
* **最佳实践**：
    * **加盐（Salt）**：为每个用户生成随机盐值，与密码组合后 Hash，防止彩虹表攻击。
    * **慢哈希算法**：使用 **BCrypt**（PHP `password_hash` 默认）、**Argon2** 或 **PBKDF2**。这些算法计算缓慢，极大增加暴力破解成本。

### 什么是会话劫持（Session Hijacking）和会话固定（Session Fixation）？
* **会话劫持**：攻击者窃取了用户的 Session ID（通过 XSS、网络嗅探），从而冒充用户。
    * 防御：HTTPS、HttpOnly、定期更换 Session ID。
* **会话固定**：攻击者在用户登录前指定一个 Session ID（如通过 URL 参数），用户登录后服务器继续使用该 ID，攻击者便已知晓有效 Session ID。
    * 防御：用户登录成功后，**必须调用 `session_regenerate_id(true)`** 生成新的 Session ID。

### 文件上传漏洞如何防御？
* **风险**：上传 Webshell（恶意脚本文件），导致服务器被控制。
* **防御**：
    * **白名单检查扩展名**：只允许 jpg, png 等，禁止 php, phtml, asp 等。
    * **检查文件内容**：验证文件头（Magic Number），防止伪装。
    * **重命名文件**：上传后随机重命名，防止利用原名。
    * **存放目录权限**：上传目录禁止执行权限（Nginx 配置 `deny all` 或不解析 PHP）。
    * **使用云存储**：即存放到 OSS/S3，与应用服务器分离。

### 什么是 DDOS（分布式拒绝服务攻击）？
* **原理**：利用大量肉鸡（僵尸网络）向目标发送海量请求，耗尽带宽或系统资源，导致正常用户无法访问。
* **常见类型**：
    * **SYN Flood**：利用 TCP 三次握手缺陷，发送大量伪造源 IP 的 SYN 包，耗尽服务器连接队列。
    * **CC 攻击（Challenge Collapsar）**：模拟正常 HTTP 请求（如频繁搜索、查询数据库），耗尽服务器 CPU/内存。
    * **UDP Flood / DNS Amplification**：大流量拥塞带宽。
* **防御**：CDN 高防、流量清洗、WAF、限制单 IP 频率、启用 SYN Cookie。

### 什么是点击劫持（Clickjacking）？
* **原理**：攻击者使用透明的 iframe 覆盖在诱导按钮上，用户本以为点击的是网页内容，实际点击了 iframe 内的目标网站按钮（如关注、转账）。
* **防御**：
    * **X-Frame-Options 响应头**：
        * `DENY`：禁止任何域加载 iframe。
        * `SAMEORIGIN`：只允许同源加载。
    * **CSP**：`frame-ancestors 'self' ...`。

### 什么是中间人攻击（MITM）？
* **原理**：攻击者拦截通信双方的数据，进行窃听或篡改。常见于公共 WiFi。
* **防御**：全程 HTTPS、开启 HSTS（强制 HTTPS）、不忽略证书错误警告。

### API 接口安全设计要点
* **HTTPS**：防止传输泄密。
* **认证与授权**：使用 OAuth2 或 JWT，无状态认证。
* **限流（Rate Limiting）**：防止暴力破解和 CC 攻击。
* **参数签名**：防止请求参数被篡改（AppKey + AppSecret + Timestamp + Nonce + Data 生成签名）。
* **时效性**：请求携带时间戳，防止重放攻击。

### 安全进阶面试题

#### 1. 什么是时序攻击（Timing Attack）？如何防御？
* **原理**：攻击者通过观察系统处理不同输入所需的时间差异，推断出内部逻辑或秘密信息（如密码/签名）。
* **场景**：字符串比较。如果是逐字比较，一旦发现字符不匹配就立即返回，那么前缀匹配越长，耗时越久。
* **防御**：使用**恒定时间比较算法**。
    * PHP 中使用 `hash_equals($known_string, $user_string)` 而非 `==` 或 `===`。

#### 2. CORS（跨域资源共享）与 CSRF 的关系是什么？
* **误区**：很多人认为配置了 CORS 就能防 CSRF，其实不然。
* **CORS**：主要限制**浏览器能否读取**跨域请求的响应结果（解除同源策略的限制），它不阻止浏览器**发送**请求。
* **CSRF**：攻击核心是利用 Cookie 自动发送请求产生副作用（如转账）。即使 CORS 阻止了读取响应，转账操作可能已经由服务器执行完毕。
* **结论**：CORS 是为了浏览器安全共享数据，CSRF 防御需要 Token 或 SameSite Cookie。

#### 3. JWT（JSON Web Token）有哪些常见安全漏洞？
* **算法被修改为 None**：攻击者将 Header 中的 `alg` 改为 `none`，并去掉签名，如果后端未强制校验算法，可能导致绕过验证。
* **密钥爆破**：如果 HMAC 密钥太弱（如 "123456"），攻击者可本地离线暴力破解签名。
* **敏感信息泄露**：Payload 只是 Base64 编码而非加密，**绝对不能存放密码**或其他敏感数据。
* **无法强制失效**：JWT 签发后在有效期内一直有效（这也是它最大的缺点），登出或改密后需配合黑名单（Redis）才能实现失效。

#### 4. 什么是 HSTS（HTTP Strict Transport Security）？
* **作用**：强制浏览器只通过 HTTPS 访问网站，防止 SSL 剥离攻击（SSL Stripping）。
* **机制**：服务器响应头 `Strict-Transport-Security: max-age=31536000; includeSubDomains`。
* **效果**：浏览器收到后，在 `max-age` 时间内，即使用户输入 `http://` 也会在本地强制内部跳转到 `https://`，不再发起不安全的 HTTP 请求。

#### 5. 什么是 CSP（内容安全策略）的 nonce 和 strict-dynamic？
* **背景**：传统 CSP 依赖白名单域名，但维护困难且容易被绕过（如 CDN 上有易受攻击的库）。
* **Nonce（随机数）**：给每个合法 `<script>` 标签加一个随机属性（如 `<script nonce="R4nd0m">`），CSP 头指定该 nonce，只有匹配的脚本才执行。
* **Strict-dynamic**：允许受信任的脚本（带 nonce）动态加载其他脚本，简化配置。

#### 6. 密码加盐（Salt）应该放在哪里？数据库里安全吗？
* **存放**：盐值通常是明文存储在数据库中（通常和 Hash 放在一起，如 `$2y$10$SaltString...HashString`）。
* **安全性**：盐的目的**不是保密**，而是**唯一性**。只要每个用户的盐不同，攻击者就无法使用彩虹表（批量查询表）进行批量破解，必须针对每个用户单独进行暴力计算，极大增加了破解成本。


### 安全面试题库大全 (补充精选)

#### 1. Web 安全 (OWASP Top 10)
1.  **OWASP Top 10 是什么？** 开放式 Web 应用程序安全项目每年发布的十大最关键 Web 应用安全风险。
2.  **什么是 SQL 注入？** 恶意 SQL 拼接到查询中执行。
3.  **什么是盲注（Blind SQL Injection）？** 页面不回显错误信息，通过时间延迟（Time-based）或布尔结果（Boolean-based）推断数据。
4.  **如何防御 SQL 注入？** 预处理语句（参数化查询）、ORM、输入验证。
5.  **什么是 XSS（跨站脚本）？** 注入恶意 JS 脚本在受害者浏览器执行。
6.  **XSS 分类？** 反射型（URL）、存储型（数据库）、DOM 型（前端逻辑）。
7.  **如何防御 XSS？** 输出编码（HTML Entity Encode）、CSP、HttpOnly Cookie。
8.  **什么是 CSRF（跨站请求伪造）？** 利用用户已登录身份发送恶意请求。
9.  **CSRF 防御？** Token 验证、SameSite Cookie、Referer 校验。
10. **什么是 SSRF（服务端请求伪造）？** 攻击者控制服务端发起请求（攻击内网）。
11. **SSRF 防御？** 限制协议（仅 HTTP）、IP 白名单（禁止内网 IP）、禁止 302 跳转。
12. **什么是 XXE（XML 外部实体注入）？** 利用 XML 解析器加载外部实体读取文件。
13. **XXE 防御？** 禁用外部实体解析（`libxml_disable_entity_loader(true)`）。
14. **什么是文件上传漏洞？** 上传 Webshell 脚本执行。
15. **文件上传防御？** 白名单后缀、MIME 校验、重命名、非 Web 目录存储、云存储。
16. **什么是 IDOR（不安全的直接对象引用）？** 修改 URL 中的 ID 访问他人数据（越权）。
17. **如何防御 IDOR？** 后端权限校验（Current User Check）、使用 UUID。
18. **什么是反序列化漏洞？** 反序列化恶意构造的数据触发魔术方法执行代码。
19. **如何防御反序列化漏洞？** 避免反序列化用户输入，使用 JSON 代替 Serialize。
20. **什么是命令注入？** `system()`, `exec()` 执行了恶意 Shell 命令。

#### 2. 网络安全
21. **DDOS 攻击类型？** SYN Flood, UDP Flood, CC (HTTP Flood), DNS Amplification。
22. **SYN Flood 原理？** 发送大量伪造源 IP 的 SYN 包，占满服务端半连接队列。
23. **防御 SYN Flood？** SYN Cookie、防火墙、增大 Backlog。
24. **CC 攻击原理？** 模拟正常请求消耗服务器 CPU/IO。
25. **防御 CC？** 验证码、WAF、限流、IP 黑名单。
26. **什么是 MITM（中间人攻击）？** 拦截篡改通信数据（如 ARP 欺骗、恶意外挂热点）。
27. **如何防御 MITM？** 全程 HTTPS、HSTS、VPN。
28. **什么是 DNS 劫持？** DNS 服务器返回错误 IP。
29. **什么是 DNS 污染？** 运营商或 GFW 抢答 DNS 请求。
30. **什么是 ARP 欺骗？** 局域网内伪造 MAC 地址，截获流量。

#### 3. 系统与主机安全
31. **Linux 权限 755 代表什么？** Owner(RWX), Group(RX), Others(RX)。
32. **什么是 SetUID？** 允许用户以文件拥有者权限执行文件（如 passwd 命令）。
33. **如何查看当前登录用户？** `w`, `who`, `last`。
34. **如何查看开放端口？** `netstat -tulpn` 或 `ss -tulpn`。
35. **如何加固 SSH？** 禁用 Root 登录、修改默认端口、使用密钥认证、Fail2Ban。
36. **什么是 Webshell？** 网页后门脚本。
37. **如何检测 Webshell？** 文件完整性监控、流量分析、Web日志分析（异常 POST）。
38. **什么是提权？** 利用漏洞从普通用户获取 Root 权限。
39. **什么是 0-day 漏洞？** 官方尚未发布补丁的漏洞。
40. **什么是 HoneyPot（蜜罐）？** 诱捕系统，记录攻击行为。

#### 4. 加密与证书
41. **对称加密 vs 非对称加密？** 对称速度快、密钥分发难；非对称安全、速度慢。
42. **AES 是什么？** 对称加密标准。
43. **RSA 是什么？** 非对称加密算法（基于大数分解）。
44. **ECC 是什么？** 椭圆曲线加密，密钥更短更安全。
45. **什么是 Hash（摘要）？** 不可逆映射（MD5, SHA256）。用途：验签、密码存储。
46. **MD5 安全吗？** 不安全，易碰撞，已被破解。
47. **什么是彩虹表？** 预计算的 Hash 链表，用于快速查表破解密码。
48. **什么是加盐（Salt）？** 密码拼接随机串再 Hash，防御彩虹表。
49. **HTTPS 握手流程？** ClientHello -> ServerHello(证书) -> KeyExchange -> Finished。
50. **什么是 CA？** 数字证书认证机构。
51. **证书包含什么？** 公钥、颁发者、有效期、域名、CA 签名。
52. **什么是 SSL Pinning？** 客户端锁定服务端证书指纹，防止伪造证书抓包。

#### 5. 认证与授权
53. **Authentication vs Authorization？** 认证（你是谁），授权（你能做什么）。
54. **Session 认证？** 服务端存状态，客户端存 Cookie(SessionID)。
55. **Token 认证（JWT）？** 无状态，客户端存 Token，服务端验签。
56. **JWT 结构？** Header.Payload.Signature。
57. **JWT 的缺点？** 无法强制注销（需黑名单）、体积大。
58. **OAuth 2.0 是什么？** 授权标准（第三方登录）。
59. **OAuth 2.0 四种模式？** 授权码（最常用）、隐式、密码、客户端凭证。
60. **什么是 SSO（单点登录）？** 一处登录，多处系统可用（CAS, SAML, OIDC）。
61. **什么是 MFA/2FA？** 多因素认证（密码 + 短信/谷歌验证器）。
62. **RBAC 模型？** 基于角色的访问控制（User-Role-Permission）。

#### 6. 业务安全
63. **如何防止短信轰炸？** 图形验证码、60秒冷却、IP/手机号限流。
64. **如何防止撞库？** 异地登录提醒、密码错误锁定、WAF。
65. **如何防止薅羊毛？** 设备指纹、风控系统、行为分析。
66. **订单金额篡改？** 后端校验金额，签名验证。
67. **重放攻击防御？** 请求带 Timestamp 和 Nonce，服务端缓存验证。
68. **验证码安全？** 仅一次有效，复杂度适中。
69. **敏感数据脱敏？** 身份证/手机号中间打码，日志不打明文密码。
70. **接口幂等性？** 唯一 RequestID，数据库唯一索引。

#### 7. 移动端安全
71. **APK 反编译？** apktool, dex2jar, jd-gui。
72. **防御反编译？** 代码混淆（ProGuard）、加壳。
73. **Android 签名校验？** 防止应用被二次打包篡改。
74. **WebView 漏洞？** 远程代码执行（`addJavascriptInterface` 早期漏洞）。
75. **Root/越狱检测？** 检测 su 文件、特定 App。

#### 8. 安全工具与审计
76. **Nmap？** 端口扫描。
77. **Wireshark？** 网络抓包分析。
78. **Burp Suite？** 抓包改包工具。
79. **SQLMap？** 自动化 SQL 注入工具。
80. **Metasploit (MSF)？** 漏洞利用框架。
81. **Nessus？** 漏洞扫描。
82. **AWVS？** Web 漏洞扫描。
83. **Hydra？** 暴力破解。
84. **Kali Linux？** 渗透测试专用系统。
85. **代码审计（Code Review）重点？** 输入入口、SQL执行点、文件操作点、敏感函数。

#### 9. 新型攻击与防御
86. **供应链攻击？** 污染依赖包（npm/composer），植入后门。
87. **防御供应链攻击？** 锁定版本（lock file），使用私有源，代码扫描。
88. **点击劫持（Clickjacking）？** iframe 透明层。防御：`X-Frame-Options`。
89. **JSON 劫持？** 利用 JSONP 漏洞或数组原型重写（较老）。
90. **HSTS？** 强制 HTTPS。
91. **CSP (Content Security Policy)？** 限制资源加载源，防 XSS。
92. **WAF (Web Application Firewall)？** 拦截常见 Web 攻击流量。
93. **RASP (Runtime Application Self-Protection)？** 应用运行时自我保护（Hook 底层函数）。
94. **社会工程学？** 利用人性弱点（诈骗、伪装）获取信息。
95. **侧信道攻击？** 利用时间、功耗、电磁泄露推断密钥（如时序攻击）。

#### 10. 综合场景
96. **如果服务器被黑了怎么办？** 
    *   断网（止损）。
    *   排查进程、端口、账号。
    *   分析日志（系统日志、Web 日志）。
    *   备份数据，重装系统（最稳妥）。
    *   修补漏洞。
97. **如何设计一个安全的登录接口？**
    *   HTTPS。
    *   密码前端 Hash（防明文传输）+ 后端 Bcrypt。
    *   验证码。
    *   错误提示模糊（“账号或密码错误”）。
    *   限制失败次数。
98. **CDN 能防御 DDOS 吗？** 可以，隐藏真实 IP，分发流量，清洗恶意流量。
99. **API 接口签名机制？** `Sign = MD5(Sort(Params) + AppSecret)`。
100. **DevSecOps 是什么？** 将安全融入 DevOps 流程（开发前、开发中、部署后）。


