### api为什么会变慢
* API变慢的原因可能涉及多个方面，以下是常见原因及优化方法，尽量简洁清晰：
* 服务器性能不足： 高并发请求导致CPU、内存或I/O瓶颈。 数据库查询效率低，复杂查询或索引缺失。
* 网络延迟： 客户端与服务器间网络不稳定或距离远。 DNS解析慢或负载均衡配置不当。
* 代码效率低： 未优化的算法或逻辑复杂，导致处理时间长。 阻塞操作（如同步I/O）拖慢响应。
* 数据库瓶颈： 查询未优化，缺少索引或数据量过大。 数据库连接池配置不当。
* 第三方依赖： 外部API或服务响应慢，影响整体性能。
* 缓存缺失： 频繁访问相同数据但未使用缓存。
* 不合理的架构设计： 单体架构难以应对高负载。缺乏异步处理或任务队列。

```
优化方法

服务器优化：

升级硬件：增加CPU、内存或使用更快的存储（如SSD）。
负载均衡：通过Nginx、HAProxy或云服务（如AWS ELB）分发流量。
水平扩展：部署多台服务器，使用集群处理请求。


网络优化：

CDN加速：静态资源或API响应通过CDN缓存，减少延迟。
DNS优化：使用可靠的DNS服务，减少解析时间。
压缩数据：启用Gzip或Brotli压缩响应数据。


代码优化：

异步处理：使用异步框架（如Node.js、FastAPI）或异步任务队列（如Celery、RabbitMQ）。
优化算法：分析代码瓶颈，减少时间复杂度。
连接池：合理配置数据库或HTTP客户端连接池。


数据库优化：

索引优化：为频繁查询的字段添加索引。
查询优化：避免SELECT *，使用精准字段；优化JOIN操作。
分库分表：数据量大时，考虑分片或读写分离。
缓存：使用Redis、Memcached缓存热点数据，减少数据库压力。


第三方依赖：

超时控制：设置合理的超时时间，避免等待过长。
降级策略：当第三方服务不可用时，切换到备用方案或返回默认数据。
异步调用：将第三方请求放入异步任务队列。


缓存策略：

引入缓存：对频繁访问的数据使用Redis或本地缓存。
缓存预热：服务启动时预加载热点数据。
失效策略：设置合理TTL，及时更新缓存。


架构优化：

微服务：拆分单体架构，模块化部署。
消息队列：用Kafka、RabbitMQ处理高并发任务。
服务治理：使用限流、熔断机制（如Hystrix、Resilience4j）防止雪崩。


监控与分析：

性能监控：使用工具（如New Relic、Prometheus）监控API响应时间、错误率。
日志分析：通过ELK或Sentry定位慢查询或瓶颈。
压力测试：用JMeter、Locust模拟高并发，找出性能瓶颈。
```

### HTTP 协议进阶面试题

#### 1. HTTP/1.1、HTTP/2、HTTP/3 的主要区别是什么？
* **HTTP/1.1**：
    * **长连接**：默认开启 Connection: keep-alive。
    * **队头阻塞**：同一连接只能串行处理请求，前一个请求慢会阻塞后续请求。
    * **文本协议**：Header 和 Body 都是文本，解析开销大。
* **HTTP/2**：
    * **多路复用**：在同一个 TCP 连接上并发处理多个流（Stream），解决了应用层的队头阻塞。
    * **二进制分帧**：传输单位是 Frame，解析更高效。
    * **头部压缩 (HPACK)**：使用索引表压缩 Header，减少冗余数据传输。
    * **服务端推送 (Server Push)**：服务端可主动推送资源（如 CSS/JS）。
* **HTTP/3**：
    * **基于 QUIC (UDP)**：彻底解决了 TCP 层的队头阻塞。
    * **0-RTT 建连**：更快的握手速度。
    * **连接迁移**：基于 Connection ID，IP 变化不中断连接。

#### 2. HTTP Keep-Alive (长连接) 是如何工作的？有什么优缺点？
* **原理**：请求结束后 TCP 连接不关闭，后续请求复用该连接。
* **优点**：减少了 TCP 三次握手和 SSL 握手的开销，降低延迟。
* **缺点**：长时间占用服务器连接资源。需要合理设置 `Keep-Alive: timeout` 和 `max`（最大请求数）。

#### 3. 301 Moved Permanently 和 302 Found 有什么区别？307 呢？
* **301 (永久重定向)**：
    * 浏览器会缓存重定向结果。后续访问旧 URL，浏览器直接跳转新 URL，不请求服务器。
    * SEO 友好，权重转移到新 URL。
* **302 (临时重定向)**：
    * 浏览器不缓存（除非指定 Cache-Control）。每次访问旧 URL 都会请求服务器。
    * 历史遗留问题：某些客户端会将 POST 请求在重定向时转为 GET（导致 body 丢失）。
* **307 (临时重定向)**：
    * 严格版的 302。强制客户端保持原有的请求方法（POST 重定向后仍是 POST）。

#### 4. 什么是 HTTP 队头阻塞 (Head-of-Line Blocking)？
* **HTTP/1.1**：一个 TCP 连接上，请求 A 没处理完，请求 B 必须等待。
* **TCP 层**：一个 TCP 包丢失，操作系统内核会等待重传，上层应用无法读取后续已到达的包（HTTP/2 虽然解决了应用层阻塞，但仍受 TCP 层阻塞影响）。

#### 5. Cookie 和 Session 的区别与联系？
* **存储位置**：Cookie 存客户端（浏览器），Session 存服务端（内存/Redis/DB）。
* **安全性**：Cookie 易被窃取/篡改（需 HttpOnly/Secure），Session ID 相对隐蔽。
* **联系**：Session 通常依赖 Cookie 来传递 Session ID（也可以用 URL 重写）。

#### 6. GET 和 POST 的本质区别？
* **语义**：GET 获取资源（幂等、安全），POST 处理/提交资源（非幂等）。
* **缓存**：GET 默认可缓存，POST 默认不可缓存。
* **参数**：GET 参数在 URL 中（长度受限，不安全），POST 参数在 Body 中。
* **本质**：都是 TCP 链接。GET 产生一个 TCP 包；POST 某些浏览器可能产生两个 TCP 包（先发 Header 再发 Body，但非绝对）。


### HTTP 协议进阶面试题

#### 1. HTTP/1.1、HTTP/2、HTTP/3 的主要区别是什么？
* **HTTP/1.1**：
    * **长连接**：默认开启 Connection: keep-alive。
    * **队头阻塞**：同一连接只能串行处理请求，前一个请求慢会阻塞后续请求。
    * **文本协议**：Header 和 Body 都是文本，解析开销大。
* **HTTP/2**：
    * **多路复用**：在同一个 TCP 连接上并发处理多个流（Stream），解决了应用层的队头阻塞。
    * **二进制分帧**：传输单位是 Frame，解析更高效。
    * **头部压缩 (HPACK)**：使用索引表压缩 Header，减少冗余数据传输。
    * **服务端推送 (Server Push)**：服务端可主动推送资源（如 CSS/JS）。
* **HTTP/3**：
    * **基于 QUIC (UDP)**：彻底解决了 TCP 层的队头阻塞。
    * **0-RTT 建连**：更快的握手速度。
    * **连接迁移**：基于 Connection ID，IP 变化不中断连接。

#### 2. HTTP Keep-Alive (长连接) 是如何工作的？有什么优缺点？
* **原理**：请求结束后 TCP 连接不关闭，后续请求复用该连接。
* **优点**：减少了 TCP 三次握手和 SSL 握手的开销，降低延迟。
* **缺点**：长时间占用服务器连接资源。需要合理设置 `Keep-Alive: timeout` 和 `max`（最大请求数）。

#### 3. 301 Moved Permanently 和 302 Found 有什么区别？307 呢？
* **301 (永久重定向)**：
    * 浏览器会缓存重定向结果。后续访问旧 URL，浏览器直接跳转新 URL，不请求服务器。
    * SEO 友好，权重转移到新 URL。
* **302 (临时重定向)**：
    * 浏览器不缓存（除非指定 Cache-Control）。每次访问旧 URL 都会请求服务器。
    * 历史遗留问题：某些客户端会将 POST 请求在重定向时转为 GET（导致 body 丢失）。
* **307 (临时重定向)**：
    * 严格版的 302。强制客户端保持原有的请求方法（POST 重定向后仍是 POST）。

#### 4. 什么是 HTTP 队头阻塞 (Head-of-Line Blocking)？
* **HTTP/1.1**：一个 TCP 连接上，请求 A 没处理完，请求 B 必须等待。
* **TCP 层**：一个 TCP 包丢失，操作系统内核会等待重传，上层应用无法读取后续已到达的包（HTTP/2 虽然解决了应用层阻塞，但仍受 TCP 层阻塞影响）。

#### 5. Cookie 和 Session 的区别与联系？
* **存储位置**：Cookie 存客户端（浏览器），Session 存服务端（内存/Redis/DB）。
* **安全性**：Cookie 易被窃取/篡改（需 HttpOnly/Secure），Session ID 相对隐蔽。
* **联系**：Session 通常依赖 Cookie 来传递 Session ID（也可以用 URL 重写）。

#### 6. GET 和 POST 的本质区别？
* **语义**：GET 获取资源（幂等、安全），POST 处理/提交资源（非幂等）。
* **缓存**：GET 默认可缓存，POST 默认不可缓存。
* **参数**：GET 参数在 URL 中（长度受限，不安全），POST 参数在 Body 中。
* **本质**：都是 TCP 链接。GET 产生一个 TCP 包；POST 某些浏览器可能产生两个 TCP 包（先发 Header 再发 Body，但非绝对）。